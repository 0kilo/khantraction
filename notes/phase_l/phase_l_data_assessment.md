# Phase L Data Assessment - Refreshed Proxy Outputs

**Date:** 2026-04-02  
**Status:** Complete after audit refresh

## 1. Output package

The refreshed Phase L solution directory now contains the active audited evidence files:

- `bulk_emission_scan.csv`
- `vary_theta_phi_fixed_rho_fixed.csv`
- `vary_phi_theta_fixed_rho_fixed.csv`
- `vary_rho_theta_fixed_phi_fixed.csv`
- `theta_phi_rho_fixed.csv`
- `theta_rho_phi_fixed.csv`
- `phi_rho_theta_fixed.csv`
- `omega_blindness_check.csv`
- `sample_packet_trajectory.csv`
- `summary.json`
- `summary.md`

These are the files that should be used to interpret the current runtime. Older files in the directory that are not regenerated by the active script are legacy leftovers and were not used as audit evidence.

## 2. What the regenerated data shows

### 2.1 Bulk landscape

The new bulk scan has:

- `8000` sampled states
- minimum proxy flux `0.0109358497518995`
- maximum proxy flux `36.54432590691133`
- mean proxy flux `5.964330432656307`

The maximum occurs where:

- `|theta|` is large,
- `|rho|` is large,
- and the sampled `phi` lies near a sheet-like value where the gate is largest.

### 2.2 One-dimensional slices

The refreshed 1D slices have widths:

- `theta`: `7.176520944773142`
- `phi`: `4.644034634479581`
- `rho`: `7.176520944773143`

This matches the active formula:

- `theta` and `rho` enter quadratically,
- `phi` enters through the multiplicative gate `1 - 0.9 * |cos(2phi)|`.

### 2.3 Two-dimensional slices

The refreshed 2D slices show:

- `theta-phi` max `19.07081262061835`
- `theta-rho` max `14.354506485657463`
- `phi-rho` max `23.47176937922259`

These tables are useful for locating high-flux proxy regions, but they still reflect the explicit algebraic model rather than a solved pinch instability.

### 2.4 Omega check

The new `omega_blindness_check.csv` is exactly flat:

- all `8` sampled `w` values produce `e_flux = 1.906457892626382`
- span `0.0`

This should be interpreted carefully. It means the active runtime is `omega`-blind by design, not that masslessness has been derived from first principles.

### 2.5 Packet trajectory

The packet trajectory is a translated Gaussian:

- starts at `pos = 1.0`
- peaks at `t = 0.5`, `pos = 2.0`
- ends at `t = 0.99`, `pos = 2.98`

This is a useful visualization of the proxy packet. It is not a solved radiative wave equation.

## 3. Correct reading of the data

The regenerated data supports three narrow statements:

1. The active Phase L proxy has a real full-domain shedding landscape.
2. That landscape is strongest for large `theta`, large `rho`, and sheet-like `phi`.
3. The package contains a manually decoupled packet diagnostic and a manually `omega`-blind emitted-flux rule.

The data does not support:

1. a direct proof of dynamical topological budding,
2. a proof of emergent masslessness,
3. or a direct discrete transition budget tied to audited Phase H spectral levels.
