# Phase C Solution Index

This directory is the refreshed audited Phase C solution package.

## Subdirectories

- `phase_c_angular_traits/`
  - Active Phase C exploratory symmetry-breaking runtime outputs, including representative seeds, zero-derivative angle-only anchor checks, and 1D/2D slice studies on the active unquotiented domain.
- `phase_c_mc_equations/`
  - Placeholder solution directory for equation-level materials. It contains no generated datasets in the current tree and is not used as evidence in the refreshed Phase C closure summary.

## Interpretation

Use `phase_c_angular_traits/` as the primary evidence directory for Phase C.

The refreshed Phase C claim is narrower than the older wording:
- the exploratory symmetry-broken runtime does show angle-sensitive trait differentiation,
- phi is the dominant driver,
- rho has moderate standalone effect,
- theta alone is weak on the audited 1D slice,
- and the strongest phi-rich representative states terminate early on the horizon event rather than surviving to the full `r_max = 20` interval.
